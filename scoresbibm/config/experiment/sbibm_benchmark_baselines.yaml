# @package _global_
name: benchmark

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      method: npe,nle,nre,nspe
      task: gaussian_linear,gaussian_mixture,two_moons,slcp
      task.num_simulations: 1000,10000,100000
      partition: cuda
      device: cuda